enum StatusCode {
  badRequest = 400,
  ok = 200,
  internalServerError = 500,
}

enum MessagesStatus {
  internalServerError = 'Internal server error',
}

export {
  StatusCode,
  MessagesStatus,
};
